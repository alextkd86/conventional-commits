/**
 * Add a + b
 * @param {*} a 
 * @param {*} b 
 * @returns 
 */
const add = (a, b) => {
    return a + b;
};

/**
 * Subtract a - b
 * @param {*} a 
 * @param {*} b 
 * @returns 
 */
const subtract = (a, b) => {
    return a -b;
}

export { add, subtract };